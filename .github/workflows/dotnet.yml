name: .NET CI (deprecated)

# This workflow is deprecated and kept only as a reusable workflow to avoid duplicate CI runs.
# Use ".NET CI (tests + coverage)" in .github/workflows/test.yml instead.

on:
  workflow_call:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use .github/workflows/test.yml instead."
